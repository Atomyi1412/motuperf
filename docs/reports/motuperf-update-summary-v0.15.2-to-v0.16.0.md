# MoTuPerf v0.15.2 至 v0.16.0 更新说明

## v0.16.0

- 新增可选的“缩放曲线”查看模式，默认关闭，原有点击/拖动定位时间的操作保持不变。
- 勾选后可在任意曲线拖动框选时间区间，所有指标曲线同步缩放；最小可视区间为 60 秒。
- 增加曲线视图还原按钮；取消勾选或还原只恢复显示范围，不改变采集数据、统计结果、截图、导出文件或现场文件内容。
- 缩放后的视口在实时采集期间保持固定，新增样本继续正常采集，不自动抢回最新位置。
- 当前可见区间的纵轴按真实数据重新计算，保留真实峰值、瞬时低帧和断点，不做平滑或插值。
- 版本号升级为 `v0.16.0`。
